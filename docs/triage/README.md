# Triage 模組文檔

- A-handshake-design.md：A 握手（Triage）設計最終版
- CandidateLite.schema.json：從 PO_Bot 複製（保持對齊）
- triage_batch_resp.schema.json：從 PO_Bot 複製（保持對齊）

使用建議：
- CLI 預設直呼內部庫；REST 僅供預覽/展示
- 設定檔位於 `config/triage_client.yaml`；安全憑證於 `.env`（POBOT_TOKEN）
